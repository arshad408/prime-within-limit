#inlude<stdio.h>
#include<conio.h>
void main()
{
int a,b,fact,j,i;
printf("enter the two limits");
scanf("%d %d",&a,&b);
for(i=1;i>=a,i<=b;i++)
{
 fact=0;
        for(j=1; j<=n; j++)
        {
            if(i%j==0)
                fact++;
        }
        if(fact==2)
            printf("%d " ,i);
    }

}
getch();
}
